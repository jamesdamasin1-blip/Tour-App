import { Dimensions } from 'react-native';

const { height: SCREEN_HEIGHT } = Dimensions.get('window');

export const BUDGET_MODAL_MAX_WIDTH = 440;
export const BUDGET_MODAL_HEIGHT = SCREEN_HEIGHT * 0.72;
export const BUDGET_MODAL_RADIUS = 40;
export const BUDGET_MODAL_PADDING = 16;
